Next.js frontend for Bibliopa — the web interface to the catalogue database.

Requires `frontend/.env.local` with `DATABASE_URL` (Postgres connection string).

    npm install
    npm run dev        # http://localhost:3000

Deployed on Vercel. Project overview: see the repository README.
